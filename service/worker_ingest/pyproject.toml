[project]
name = "astro-reason-worker-ingest"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "redis>=5.0",
  "rq>=1.16",
  "boto3>=1.35",
  "psycopg[binary]>=3.2",
  "lxml>=5.3",          # for XML parsing later
]
